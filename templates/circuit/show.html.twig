{% extends 'base/one.column.html.twig' %}

{% set title %}Circuit {{ circuit }}{% endset %}

{% block body %}
    <h5>Participants</h5>
    <div class="list-group">
        {% for user in circuit.users %}
            <a class="list-group-item list-group-item-action">
                {{ participant }}
            </a>
        {% endfor %}
    </div>
{% endblock %}
