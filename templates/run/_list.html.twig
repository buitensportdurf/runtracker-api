<div class="list-group">
    {% for run in runs %}
        <a class="list-group-item list-group-item-action"
           href="{{ path('app_front_run_show', {id: run.id}) }}">
            {{ run }}
        </a>
    {% endfor %}
</div>