{% extends 'base/base.html.twig' %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-sm mx-auto">
                {% if title is defined %}<h3>{{ title }}</h3>{% endif %}
                {% block body %}{% endblock %}
            </div>
        </div>
    </div>
{% endblock %}